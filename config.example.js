// Copy this file to config.js and paste your Apps Script web app URL.
// config.js is gitignored. On Vercel, set the API_URL environment variable
// instead and the build writes config.js for you.
// Leave API_URL empty to run in demo mode (data stays in this browser).
window.APP_CONFIG = {
  API_URL: ''
};
