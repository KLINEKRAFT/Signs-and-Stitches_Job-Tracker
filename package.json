{
  "name": "signs-and-stitches-job-tracker",
  "private": true,
  "description": "Shared job tracker for Signs & Stitches. Plain HTML/CSS/JS; the Google Sheet is the database.",
  "scripts": {
    "start": "npx --yes serve -l 8080 .",
    "test": "node --test tests/*.test.js",
    "build": "node scripts/vercel-build.js"
  }
}
